services:
  db:
    image: pgvector/pgvector:pg16
    container_name: rag_db
    environment:
      POSTGRES_USER: chunkie
      POSTGRES_PASSWORD: dyjdyj123
      POSTGRES_DB: ragdb
    ports:
      - "5432:5432"
    volumes:
      - rag_db_data:/var/lib/postgresql/data

volumes:
  rag_db_data: